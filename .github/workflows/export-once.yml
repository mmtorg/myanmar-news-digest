# .github/workflows/export-once.yml
name: Export all articles CSV (manual)

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref to checkout (branch or tag)"
        required: false
        default: "export_csv"
      start:
        description: "Start date in MMT (YYYY-MM-DD)"
        required: true
        default: "2025-08-23"
      out:
        description: "Output CSV filename (optional)"
        required: false
        default: ""

jobs:
  run:
    runs-on: ubuntu-latest
    environment: export_csv # ← ここ！環境のSecretsを使う
    timeout-minutes: 360
    concurrency: # ← 二重起動で無料枠を食い潰さない
      group: export-csv-manual
      cancel-in-progress: false
    env:
      # 出力ファイル名（out未指定なら start から自動命名）
      OUT: ${{ inputs.out != '' && inputs.out || format('articles_since_{0}_MMT.csv', inputs.start) }}

      # ===== Gemini API キー（export_csv 環境のSecretsを使用） =====
      GEMINI_API_SUMMARY_KEY: ${{ secrets.GEMINI_API_EXPORT_SUMMARY_KEY }}
      GEMINI_API_DEDUPE_KEY: ${{ secrets.GEMINI_API_EXPORT_DEDUPE_KEY }}

      # ===== レート制御（必要に応じて数値を調整） =====
      GEMINI_REQS_PER_MIN: "20" # 1分あたり最大リクエスト数（例）
      GEMINI_MIN_INTERVAL_SEC: "1.5" # 連続呼び出しの最小間隔（秒）
      GEMINI_JITTER_SEC: "0.3" # 各呼び出し前に 0〜この秒数のランダム待機（バースト緩和）

    steps:
      - name: Checkout (export_csv)
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install -U pip
          if [ -f requirements.txt ]; then python -m pip install -r requirements.txt; fi
          python -m pip install -U google-genai

      - name: Assert Gemini keys present
        run: |
          [ -n "$GEMINI_API_SUMMARY_KEY" ] || { echo "::error::GEMINI_API_SUMMARY_KEY is empty"; exit 1; }
          [ -n "$GEMINI_API_DEDUPE_KEY" ]  || { echo "::error::GEMINI_API_DEDUPE_KEY is empty";  exit 1; }

      - name: Run export
        run: |
          python export_all_articles_to_csv.py \
            --start "${{ inputs.start }}" \
            --out   "${{ env.OUT }}"

      - name: Upload CSV artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.OUT }}
          path: ${{ env.OUT }}
          if-no-files-found: error
